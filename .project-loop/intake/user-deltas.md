# Пользовательские Дельты

Проект: trackmate
Обновлено: 2026-06-29

Этот файл хранит существенные свежие корректировки, решения и изменения области от пользователя, которые приходят во время шага. Записи компактны и пополняются до отражения в источнике правды и handoff.

## Записи

### Product Delta: Personal Alert Copy For Egor

ID источника: `S023`

Приложение: `/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-10ff1d75-6327-46fd-a229-338c5f8adb6b.png`.

Исходный ввод:

```text
егору сделай чтобы ему когкретно у него юз на даблю начинается приходили короче на алерты типо забыл или не сделал там с шансом в тридцать процентов условные короче чтобы вот такое сообщение приходило егор почему ты не сделал свои дела, где твоя рутина бро ты нищий, ну форматирование эмодзи стиль все как бы как в друг сообщениях чин по чину четко классно
```

Нормализация:
- [x] требование: для Егора, у которого username начинается с `w`, добавить персональный текст в forgot/missed alerts.
- [x] требование: вероятность около 30%, но детерминированная по событию, чтобы retry не менял текст.
- [x] требование: сохранить форматирование и стиль Trackmate; тексты держать в `internal/messages/messages.md`.
- [x] ограничение: не деплоить на production в этом шаге.

Маршрутизация:
- [x] карта источников обновляется в `S023`
- [x] чеклист обновляется в `REQ-045`
- [x] план и текущий шаг переводятся в `STEP-024`
- [x] локальная реализация и focused tests выполнены
- [x] broad validation выполнена
- [x] handoff обновлен

### Product Delta: Morning Routine Window And Message Levels

ID источника: `S022`

Приложение: `/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-d3a91351-46be-495c-ab37-fc1feb9d3875.png`.

Исходный ввод:

```text
короче по рутине какие должны быть тайминги - она должна теперь приходить в 8 утра следующего дня от которого идет заполнение, то есть ты заполнил рутину и она первая должна тебе прийти в 8 утра следюущего дня, в 8 вечера следующего дня от фактического дня рутины тебе должно прийти напоминание мол бро отмеить сделал ли ты рутину за прошлый день и в 12 часов ночи след дня от рутины оно должно автоматом закрывать - типо просто закрываться и сообщение тоже должно быть лаконичным и простым вот такие тайминги

алерт что типо отметь бро висит до закрытия, а алерт что было закрытие висит еще сутки крч или пока ты не тыкнул что понял там ниче больше сдлеать нельзя

при этом надо обработать что ты можешь рутину же менять в процессе и если у тебя была одна рутину и потом ты поменял чтобы все корректно ты за день отметил прошлую рутину и за некст день уже отмечаешь следующую рутину

после отметки все сообщ пропадают только в лидерборде висит статистика стрик и тп

при этом надо четко разграничить когда бот тебя пингует а когда нет - типо пусть будет 4 уровня сообщения - первый это тихие сообщения - это должны быть все сообщения по умолчанию, это сообщения которые отправялеются без уведомления у тг есть такая функция, потом должны быть второй уровень это просто сообщения они с уведом соответственно, это по идее какие-нибудь ивенты типо апдейты и отчеты, всякие закрепы топиков и тп, при этом прогресс - это 1 уровень сообщений, потом у нас идет ответ на твое сообщение если возможно - это третий и он делается где важно надо там сегодня и тп, где уместно просто, и 4 уровень это сообщение с пингом, это если ты что-то забыл, где-то не успел не отметил и тп и так далее, в прогрессе щас отметки но их надо поменять просто на ссылку на тебя типо текст имени просто с форматированием в ссылку куда твой юз вставлен

также надо сделать чтобы в прогрессе где идет типо выполнил слово чтобы была ссылка на сообщение выполнения и в отчете если в прогрессе пишется фото или аудиои или что там чтобы была ссылка на которую можно тыкнуть и оно тебя перенесет и ты сможешь прочитать прослушать там просмотреть
```

Нормализация:
- [x] требование: routine check-in за дату `D` приходит в 08:00 `D+1`.
- [x] требование: reminder по незакрытой routine приходит в 20:00 `D+1`, висит до закрытия routine.
- [x] требование: auto-close происходит в 00:00 `D+2`; notice висит около суток или до `Понял`.
- [x] требование: после ручного закрытия routine flow messages удаляются, остается только таблица рутин.
- [x] требование: при смене routine старый список сохраняется для уже наступившей проверки, новый список применяется для следующей.
- [x] требование: явно разграничить 4 уровня Telegram-сообщений; `Прогресс` остается тихим.
- [x] требование: в `Прогресс` имя участника, действие результата и медиа-лейблы отчета должны быть кликабельными.

Маршрутизация:
- [x] карта источников обновляется в `S022`
- [x] чеклист обновляется в `REQ-042..REQ-044`
- [x] план и текущий шаг переводятся в `STEP-023`
- [x] локальная реализация и focused tests выполнены
- [x] broad validation выполнена
- [x] handoff обновлен

### Production Verification: Chat Complaints About Today Tasks

ID источника: `S021`

Приложение: `/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-fbaf6bf2-6039-4763-a2a8-b9f28bdc5526.png`.

Исходный ввод:

```text
вот этот момент тоже чекни там ярик и егор жалуются что задачи не принимает дня крч и тп
```

Нормализация:
- [x] требование: проверить production logs/DB/Telegram history по жалобам Ярика и Егора.
- [x] требование: понять, были ли задачи дня реально не приняты.
- [x] ограничение: не делать массовую чистку и не удалять обычные сообщения общего чата без отдельной причины.

Маршрутизация:
- [x] карта источников обновляется в `S021`
- [x] чеклист обновляется в `REQ-041`
- [x] план и текущий шаг переводятся в `STEP-022`
- [x] production logs/db/dumps проверены
- [x] handoff обновлен

Продовые факты:
- Скрин соответствует общему topic `Чат` 2026-06-26.
- Ярик 2026-06-26 был принят в `Сегодня`: prompt `3455`, source message `3456`, Trackmate card `3457`; итог опубликован в `Прогресс` как message `3487`.
- Егор 2026-06-26 действительно упирался в старый `uq_pending_inputs_workspace_group_id`; задача уже восстановлена как `daily_tasks.id=172`, progress message `3653`, а complaint messages `3464`/`3465` удалены в STEP-019.
- Реплика Ярика `БРО ГДЕ ЗАДАЧА ВТОРОЙ ДЕНЬ` была про Игоря: через минуту после нее Игорь добавил задачу, source message `3476`, card `3477`; итог опубликован в `Прогресс` как message `3493`.
- Current production health: `api`, `worker`, `postgres` healthy; progress outbox pending/publishing/failed = 0; Today pending input count = 0.

Итог:
- Новых DB/Telegram правок не требовалось.
- Пользовательские реплики Ярика в общем чате не удалялись: это не bot noise и не сломанный pending prompt.

### Production Verification: Today Screenshot And Current Health

ID источника: `S020`

Приложение: `/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-71043fd1-bde0-4ea5-a6f7-6c421e4103d0.png`.

Исходный ввод:

```text
вот этот моментик еще проверяем и правим и чистим и все делаем крч тут баги забагалось все

надо ваще прод починить потому что вот у меня с утра тоже не засчитал трекмейт задачу на день непонятно че там с ним он щас работает или нет
```

Нормализация:
- [x] требование: проверить production по скриншоту, логам, дампам и БД.
- [x] требование: понять, работает ли Trackmate сейчас.
- [x] требование: проверить жалобу, что задача Игоря утром 29.06 не засчиталась.
- [x] ограничение: не вносить лишние DB/Telegram правки, если текущие данные уже чистые.

Маршрутизация:
- [x] карта источников обновляется в `S020`
- [x] чеклист обновляется в `REQ-040`
- [x] план и текущий шаг переводятся в `STEP-021`
- [x] production logs/db/dumps проверены
- [x] handoff обновлен

Продовые факты:
- Скрин соответствует 2026-06-28 22:25 MSK: logs показывают `today:add` Егора и падение на legacy constraint `uq_pending_inputs_workspace_group_id`.
- Актуальный dump `Сегодня` не содержит prompts `3556`/`3558` и messages `3557`/`3559`/`3560`; они уже удалены.
- Перед этим Егор написал в `Чат` message `3555`: `сегодня я отсыпался, поэтому без задач`; поэтому task на 2026-06-28 не восстанавливался.
- Отчет Игоря `дома один, дома убрался...` уже засчитан как task `169`, progress message `3543`.
- На 2026-06-29 в `Сегодня` есть задача Ярика `171`/card `3652`; задача Игоря за 29.06 отсутствует, потому что его сообщения утром были в topic `Чат` (`thread_id=0`), а не в `Сегодня`.
- Services healthy; progress outbox pending/publishing/failed = 0; Today pending input count = 0.

Итог:
- Новых DB/Telegram правок не требовалось.
- Production сейчас работает; текущая проблема была в старом скрине до снятия legacy constraint и в том, что 29.06 сообщения Игоря пришли не в topic `Сегодня`.

### Production Cleanup: Today Topic Noise And Missed Progress

ID источника: `S019`

Приложение: `/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-f38928f4-d47d-4ede-83b3-922ce6f3a1dd.png`.

Исходный ввод:

```text
вот этот момент проверь тут прям куча мусора и ниче не засчитано у пацанов надо прям привести топик в нормальный вид все починить все вычесать
```

Нормализация:
- [x] требование: проверить production Today topic по скрину и логам.
- [x] требование: убрать видимый служебный мусор из `Сегодня`.
- [x] требование: восстановить засчитывание missed daily results в БД и `Прогресс`.
- [x] ограничение: не трогать unrelated product code и не деплоить локальную пачку.

Маршрутизация:
- [x] карта источников обновляется в `S019`
- [x] чеклист обновляется в `REQ-039`
- [x] план и текущий шаг переводятся в `STEP-020`
- [x] production backup снят
- [x] DB/progress outbox исправлены
- [x] Telegram topic cleanup выполнен через Harvest
- [x] handoff обновлен

Продовые факты:
- В `Сегодня` duplicate prompts `3460/3462/3466/3468` к моменту проверки уже отсутствовали.
- В `Прогресс` отсутствовали manual-restored events `193` и `194`: оба стояли `published`, но `published_message_id` был пустым.
- В `Сегодня` оставались service confirmations `✅ Итог сохранен`, которые не несут данных.

Итог:
- Перед правкой снят backup `/opt/trackmate/backups/trackmate_manual_today_cleanup_20260629T122958Z.dump`.
- Events `193` и `194` возвращены в `pending`; worker штатно опубликовал их как messages `3653` и `3654` в `Прогресс`.
- Через Telegram Harvest удалены service confirmations `3351,3356,3364,3394,3399,3437,3448,3485,3491,3519,3530,3541,3572`.
- Финальный dump `Сегодня` не содержит `Напиши главную задачу` и `Итог сохранен`; `Сегодня` pending input count = 0.
- Progress outbox clean: pending/publishing/failed = 0.

### Production Follow-up: Egor Task Save Bug And Voice Text

ID источника: `S018`

Приложение: `/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-4a1ae09c-c9e7-4078-b721-7fade5ba8c2a.png`.

Исходный ввод:

```text
вот это требование проверь тоже надо поправить багулечку исправили мы или нет

удалять сообщ можно через телеграм харвест и там же можно расшифровывать гс
```

Нормализация:
- [x] требование: проверить на production, почему у Егора 26.06 задача дня не сохранялась.
- [x] требование: подтвердить, исправлена ли сама причина бага.
- [x] требование: удалить показанные мусорные сообщения через Telegram Harvest.
- [x] требование: использовать Telegram Harvest/Vosk для расшифровки связанного голосового отчета, где это помогает восстановить корректный итог в БД.

Маршрутизация:
- [x] карта источников обновляется в `S018`
- [x] чеклист обновляется в `REQ-038`
- [x] план и текущий шаг переводятся в `STEP-019`
- [x] production logs/schema проверены
- [x] Telegram messages cleaned through Harvest
- [x] DB/visible Telegram messages updated without new posts
- [x] handoff обновлен

Продовые факты:
- Logs 2026-06-26 around 08:49-08:55 MSK: три попытки `today:add` для Егора упали на `duplicate key value violates unique constraint "uq_pending_inputs_workspace_group_id"`.
- Это тот же старый legacy constraint, который ломал topic-scoped pending; на production schema сейчас constraint отсутствует, остался `ux_pending_inputs_workspace_user_thread`.
- Rollback probe на production успешно вставляет два pending inputs одного пользователя в разных `message_thread_id`, значит текущая схема больше не блокирует независимые топики.
- Сообщения жалобы в общем чате `3464` и `3465` удалены через Telegram Harvest main profile; повторный dump не находит их в истории.
- Задача Егора за 2026-06-26 восстановлена ранее как `daily_tasks.id=172`, status `partial`, task message `3467`, report `я все сделал кроме бега(устал сильно)`.
- Voice `3386` для task `160` расшифрован через Harvest/Vosk; ASR шумный, но смысл восстановлен.

Итог:
- Product bug из скриншота уже исправлен текущей production schema и локальной migration `202606290001_drop_legacy_pending_input_unique.sql`.
- В `daily_tasks.id=160` и `progress_events.id=192` сохранен вычитанный voice summary: вместо бега было много кардио, около 19 тысяч шагов, день закрыт хорошо.
- Видимые сообщения `Сегодня` `3361` и `Прогресс` `3649` отредактированы без новых публикаций.
- Backup перед правкой voice-текста: `/opt/trackmate/backups/trackmate_manual_voice_transcript_20260629T121722Z.dump`.

### Review Delta: Routine Reason Prompt As Separate Temporary Message

ID источника: `S012`

Приложение: `/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-0914828b-48af-41a5-ac70-9785d8f1a187.png`.

Исходный ввод:

```text
дальше что мне не нравится - это то что механика того как сейчас устроены рутины и цели не соответсвует тому стилю и коду как устроены сегодня - там вообще другое, надо прям переиспольщзовать чтобы это было тоже самое и виделось тем же самым механизмом

смотри что я имею ввиду - вместо того чтобы бот спрашивал про конкретный пункт отдельным сообщением - он меняет основное сообщеие рутины - так быть не должно - потому что бот должен только отмечать выполнение рутины в основном сообщении и а по пункатм по каждому пункут он должен присылать отдельное сообщение если пункт не выполнен или выполнен астично то есть он должен спрашивать почему пункт не выполнен отлдельным сообщением и просить ответ чтобы было ясно и понятно как в сегодня текст а после заполнения удалять и свое сообщение и ответ пользоватеоля на него

также в рутинах бот вообще не должен спрашивать итог дня это надо убрать пусть српашивает только по пунктам - иначе это дублирование того что есть в сегодня
и после отметки всего
```

Нормализация:
- [x] требование: проверить production logs read-only и не менять production в этом шаге.
- [x] требование: менять только routine flow, потому что по целям нет отдельного конкретного сценария в текущем сообщении.
- [x] требование: основная карточка рутины только отмечает статусы пунктов и двигает чеклист.
- [x] требование: для `Нет`/`Частично` Trackmate отправляет отдельное временное сообщение с вопросом причины.
- [x] требование: после ответа пользователя Trackmate удаляет свое временное сообщение и ответ пользователя.
- [x] требование: routine final reflection убрать; после отметки всех пунктов routine check-in закрывается без дополнительного итога дня.

Маршрутизация:
- [x] карта источников обновляется в `S012`
- [x] чеклист обновляется в `REQ-030`
- [x] план и текущий шаг переводятся в `STEP-013`
- [x] production logs проверены read-only
- [x] реализация направлена в `internal/bot/routines.go`, `internal/storage/postgres/routines.go`, `internal/ui/formatters.go`, `internal/ui/keyboards.go`
- [x] тесты и E2E-шаблон обновлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- S012 уточняет/заменяет часть S001/REQ-005: final reflection в routine flow больше не нужна, потому что итог дня живет в `Сегодня`.
- S012 уточняет CON-001: антиспам сохраняется для основного check-list, но временное отдельное сообщение причины разрешено для `Нет`/`Частично` и удаляется после ответа.

Продовые факты:
- Production host `/opt/trackmate` остается на `9a58215`; текущая локальная ветка не выкачивалась.
- Logs 2026-06-24 17:00 UTC: worker отправил routine cards `3373`/`3374`; callback `routine:item:287:0:failed` пришел по message `3373`, затем user message `3375`; текущий production flow редактирует основную карточку для причины.

Итог:
- `Нет`/`Частично` теперь сразу отмечают пункт в основной карточке и отправляют отдельный временный prompt причины reply к карточке.
- После ответа пользователя Trackmate удаляет prompt причины и ответ пользователя, сохраняет причину и двигает основную карточку дальше.
- После последнего пункта routine check-in закрывается сразу без `routine_reflection` и без вопроса про итог дня.
- Старый active `routine_reflection` path удален из bot/domain/storage API; историческое поле `reflection_text` в схеме не трогалось.
- Проверки: `go test ./internal/ui ./internal/bot ./internal/app/routine ./internal/storage/postgres`, `go test ./... -count=1`, `make lint`, `loopctl.py validate`.

### Review Delta: Routine Reason Prompt Visual Consistency

ID источника: `S011`

Приложение: `/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-c88a6f65-65d8-4919-851d-90137a4b96e9.png`.

Исходный ввод:

```text
смотри щас буду присылать скриншоты там где трекмейт повел себя не так как я хотел и это надо будет проверять и править

проверять по логам надо прям на продакшене и смотреть сообщения + править эти правки и баги но только детермининрованно и только то что я сказал все сразу править и чистить не надо - будем по порядку все делать

сейчас надо проверить по коду на продакшене и исправить на локалке - пока на прод не грузим исправления всей пачкой загрузим - вот этот момент на скриншоте тут почему-то рутина по разному отображается

2 - надо изменить эмодзи который идет первым в рутине на что-то другое более приятнео глазу
```

Нормализация:
- [x] требование: проверить production logs/DB для конкретного поведения на скриншоте.
- [x] требование: не делать production deploy сейчас; исправления остаются локально до пачки.
- [x] требование: исправить только показанную проблему, не чистить и не править остальные найденные данные.
- [x] требование: prompt причины рутины должен выглядеть как обычная карточка той же рутины, включая автора и формат вопроса.
- [x] требование: заменить первый emoji в routine card/control header на более приятный визуально.

Маршрутизация:
- [x] карта источников обновляется в `S011`
- [x] чеклист обновляется в `REQ-029`
- [x] план и текущий шаг переводятся в `STEP-012`
- [x] production logs/DB проверены read-only
- [x] локальная реализация направлена в `internal/ui/formatters.go`, `internal/bot/routines.go`
- [x] тесты обновлены
- [x] проверка выполнена
- [x] handoff обновлен

Продовые факты:
- production host `/opt/trackmate` работает на `9a58215`.
- `2026-06-24 17:00:04 UTC` worker отправил routine cards `3373` и `3374` в topic `3283`.
- callback `routine:item:287:0:failed` пришел по message `3373`, затем user message `3375`; callback `routine:item:287:1:failed` пришел по тому же message `3373`.
- Разница на скриншоте вызвана `FormatRoutineReasonPrompt`: он редактировал ту же карточку, но строил текст отдельным formatter'ом без `personLabel` и без обычного формата `N/M: пункт?`.

Итог:
- Reason prompt рутины теперь переиспользует обычную карточку рутины, поэтому сохраняет автора и формат вопроса `N/M: пункт?`.
- Короткая строка `Что помешало?` добавляется к той же карточке без отдельного визуального шаблона.
- Первый emoji routine header заменен с `🔁` на `🌿`.
- Production deploy не выполнялся.
- Проверки: `go test ./internal/ui ./internal/bot`, `go test ./... -count=1`, `make lint`, `loopctl.py validate`.

### Review Delta: Routine Card Date Clarity

ID источника: `S010`

Исходный ввод:

```text
Смотри, сейчас пришли две рутины, получается, на продакшене. Вот, и такой прикол, что там у них, типа, во-первых, нет описания никакого текста или типа того, во-вторых, они просто написаны 24.06, и кажется такое впечатление, что эти рутины, они пришли за 24 число, которое только началось, а не за 23, которое уже прошло. Вот, и я сейчас даже сам уже сам не понимаю, это рутины я сейчас должен отметить за прошлый день, или это на сегодня рутина. Вот, объясни мне этот момент, вот, и добавь какое-то или объяснение, или что вот в саму карточку рутины, которая прилетает, такое лаконичное, краткое, что, типа, это там за сегодня или за какое число. Ну, то есть, можно не точку, а можно просто объяснить, что типа там в фейк 13, типа этот там отметь там свою, как прошла там твоя рутина вчера, сегодня там и так далее.
```

Нормализация:
- [x] требование: объяснить, что дата в карточке рутины означает дату самой проверки рутины.
- [x] требование: сделать карточку рутины самодостаточной и явно писать, за какую дату нужно отмечать пункты.
- [x] требование: текст должен быть лаконичным и в стиле Trackmate.

Маршрутизация:
- [x] карта источников обновляется в `S010`
- [x] чеклист обновляется в `REQ-028`
- [x] план и текущий шаг переводятся в `STEP-011`
- [x] реализация направлена в `internal/ui/formatters.go`
- [x] тесты обновлены
- [x] проверка выполнена
- [x] handoff обновлен

Итог:
- На текущем production дата `24.06` в карточке означает routine check-in за `24.06`, а не за `23.06`.
- В локальной версии карточка теперь начинается с `Рутина за DD.MM` и содержит короткую строку `Отметь, как прошел этот день`.
- Такой же смысл добавлен в prompt причины, чтобы при `Нет`/`Частично` дата не терялась.
- Проверка: `go test ./internal/ui ./internal/bot ./internal/app/routine`.

### Review Delta: Topic-Scoped Pending Inputs And Evening Routine Flow

ID источника: `S009`

Исходный ввод:

```text
Давай для всех топиков сделаем поведение, как для топика сегодня. То есть, если бот ждет там цель или рутину, то пусть он, короче, ждет, а топик в другой ввод, он игнорируется. Вот. При этом давай добавим fallback, ну, короче, проверку, что если там сутки ввода никакого нет, то бот отменяет input pending и как бы удаляет все сообщения, которые были вот как бы в процессе, да, то есть, типа, введи там или что-то там от пользователя что-то. Вот. То есть сделаем каждый топик таким изолированным, просто чтобы каждый топик вел себя одинаково. Вот. То есть мы в каждом топике пишем, вот, и то, что мы пишем в каждом топике, оно не влияет на другие топики. И если мы, допустим, в одном топике начали писать, а продолжили в другом, то у нас всё в другом так же работает и не мешает, и первый топик не мешает второму, и наоборот, ввод во второй топик не сломает там первый топик.

Не очень понимаю историю про то, что у пользователя только один незавершенный ввод на группу. Не очень понятно, зачем это. Мне кажется, такого не должно быть. Мы просто по топикам думаем, то есть внутри топика, в рамках топика. То есть, если pending input, то окей, он есть. Вот типа, допустим, с fallback там в сутки, да. Вот. Если pending input нет, то как бы другие... Вообще они как бы не должны друг на друга влиять, разные топики, они не должны игнорировать события с других топиков. Топик должен смотреть на события только со своего топика. По умолчанию. То есть, не должно быть ни ограничений, он по умолчанию должен смотреть только на события со своего топика. Топики в целом, они похожи все. То есть там код можно переиспользовать, обобщать и таким образом поддерживать качество кода. Если что-то отменяется, если trackmate удаляет там pending input, удаляет там свои сообщения, то он это должен делать абсолютно молча, без каких-либо там сообщений в ответ и так далее. То есть мы унифицируем flow, как для daily. Да, дополнительная публикация карточки с полным текстом цели не нужна. Давай потом поменяем, чтобы карточка рутины приходила... Например, давай пусть карточка, во-первых, если по времени просто проходит, то она приходит в тот же день, а не обязательно с следующего дня. Ну как бы если по времени позволяет, да, если она там раньше времени. А во-вторых, давай, наверное, сделаем, чтобы она вечером в день приходила, то есть в день, ну как бы вечером. И плюс она также её нужно было бы закрыть, допустим, до 12 часов следующего дня. То есть там в 12.00 по Москве там придёт сообщение, типа, рутина не закрыта. Ну вот как в сегодня, точно такой же flow, то есть она придёт просто вечером, вот. Потом в 12 придёт сообщение, что типа закрою рутину, а потом в 12 часов следующего дня он тебе типа сам закроет, что она не выполнена, грубо говоря.
```

Нормализация:
- [x] требование: `pending_inputs` должны быть изолированы по теме: один пользователь может иметь независимый незавершенный ввод в разных топиках.
- [x] требование: сообщения из другой темы не отменяют и не ломают pending текущей темы; обработчик смотрит только pending той темы, где пришло сообщение/callback.
- [x] требование: убрать глобальный запрет "один pending input на workspace/user".
- [x] требование: pending input старше 24 часов тихо отменяется worker'ом; Trackmate удаляет свой prompt и известные сообщения пользователя из процесса без callback/message ответа.
- [x] требование: настройка целей остается без большой карточки с полным текстом целей.
- [x] требование: routine check-in приходит вечером, в день настройки тоже, если рутина настроена до вечернего времени.
- [x] требование: незакрытая рутина получает напоминание после конца дня и автоматически закрывается в 12:00 следующего дня как невыполненная.

Маршрутизация:
- [x] карта источников обновляется в `S009`
- [x] чеклист обновляется в `REQ-025..REQ-027`
- [x] план и текущий шаг переводятся в `STEP-010`
- [x] реализация направлена в рабочий шаг
- [x] тесты добавлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- `S008` требовал сброса setup-черновиков при переходе между `Рутины` и `Цели`; новая дельта `S009` выше по приоритету и заменяет это поведение изоляцией по теме без сброса чужого топика.

Итог:
- `pending_inputs` теперь имеют явный `message_thread_id` и уникальность `workspace/user/thread`;
- сообщения и callbacks в одном топике не блокируются pending из другого топика;
- wrong-topic ввод больше ничего не удаляет и не сбрасывает;
- worker тихо удаляет pending старше 24 часов и удаляет сохраненные prompt/user message IDs;
- рутина приходит после 20:00; если список сохранен до 20:00, первая карточка может прийти в тот же день;
- незакрытая рутина получает напоминание после конца дня и автозакрывается в 12:00 следующего дня;
- проверки без доступной PostgreSQL: `go test ./internal/domain ./internal/storage/postgres ./internal/app/pending ./internal/app/routine ./internal/app/goals ./internal/worker ./internal/bot ./internal/ui`, `go test ./... -count=1`, `make test`, `make lint`;
- DB-backed integration tests и `TRACKMATE__DATABASE_URL=... make migrate` не выполнены полноценно: локальный PostgreSQL недоступен, `make docker-up` не смог подключиться к Docker daemon.

### Review Delta: Pending Setup Cleanup And Goals Echo

ID источника: `S008`

Исходный ввод:

```text
трекмейт нужно чтобы прошлый ввод сбрасывался рутины или цели и удалялись сообщения от трекмейта и юзера если ввод не закончен и если юзер начал делать ввод в другом топике

почему на цели трекмейт присылает большое полотно то есть переписывает твои цели в сообщении. только первый забив потом не присылает уже твои цели и просто пишет что цели записаны и по другому отвечает - надо одним способом как-то

во сколько трекмейт присылает отчет по рутинам?

исправь эти вещи
```

Нормализация:
- [x] требование: незаконченные setup-черновики `routine_plan` и `seasonal_goals` можно перебивать новым setup-вводом в `Рутины`/`Цели`.
- [x] требование: при переходе в другой топик старый prompt Trackmate удаляется, wrong-topic сообщение пользователя удаляется, pending input очищается.
- [x] требование: сохранение целей не должно публиковать большую карточку с полным текстом целей; первый и повторный ввод отвечают одинаково коротко.
- [x] требование: объяснить и явно показать время routine check-in: после 09:00 по локальному времени workspace.

Маршрутизация:
- [x] карта источников обновляется в `S008`
- [x] чеклист обновляется в `REQ-022..REQ-024`
- [x] план и текущий шаг переводятся в `STEP-009`
- [x] реализация направлена в рабочий шаг
- [x] тесты добавлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- wrong-topic behavior для daily input остается прежним: daily pending не закрывается сообщением из другой темы. S008 применяется только к setup-черновикам `Рутины`/`Цели`.

Итог:
- `routine_plan` и `seasonal_goals` теперь можно перебить новым setup-вводом в `Рутины`/`Цели`;
- при wrong-topic сообщении для такого черновика удаляются старый prompt Trackmate и сообщение пользователя, pending input очищается;
- сохранение целей больше не создает отдельную большую карточку с полным текстом целей, confirmation единый и короткий;
- старые goal card messages удаляются при следующем сохранении целей;
- routine check-in явно описан как ежедневный после 09:00 по локальному времени workspace;
- проверки: `go test ./internal/bot ./internal/ui ./internal/storage/postgres ./internal/domain ./internal/app/routine ./internal/app/goals`, `make test`, `make lint`, `loopctl.py validate`.

### Review Delta: Topic Copy Tone And Goal Questions

ID источника: `S007`

Исходный ввод:

```text
Сезонные цели на общий период - тут исправь на чтониьудь понятнее описание как в других топиках что то вроде долгосрочные цели которых мы хотим добитьсся за лето напримери

стиль предложений разный у новых и старых топиков описаний надо привести под стиль старых

маркеры - тире а не маркеры

пример формата в сезонные цели чуть лучше сделай. и лучше не пример а описание полей типо смарт что там в полях зачем

в недельном обзоре целей надо переформулировать вопросы оч старнные

🏁 <b>Итог периода: Лето 2026</b>

<b>Оценка:</b> частично

Напиши короткий вывод:
• что получилось
• что не получилось
• что берешь в следующий сезон - в итогах периода не короткий вывод а прям конкретный

вот исправление примени
```

Приложение: `/Users/igor/.codex/attachments/55bd8dc4-45f5-4892-8186-d844e12eeb30/pasted-text.txt`.

Нормализация:
- [x] требование: привести закрепы новых топиков к стилю старых: `Здесь у каждого...`, `Здесь живут...`, `Здесь собирается...`.
- [x] требование: заменить видимые маркеры списков с `•` на длинное тире `—`.
- [x] требование: описание `Цели` сделать понятнее: долгосрочные цели/ориентиры на сезон, например на лето.
- [x] требование: вместо частного примера целей дать описание полей формата: направление, результат, метрика, шаг недели, зачем.
- [x] требование: переписать вопросы недельного обзора целей на естественные и конкретные.
- [x] требование: итог периода просит конкретные результаты по целям, а не короткий вывод.
- [x] требование: парсер рутины должен принимать длинное тире, раз интерфейс предлагает такой стиль списков.

Маршрутизация:
- [x] карта источников обновляется в `S007`
- [x] чеклист обновляется в `REQ-021`
- [x] план и текущий шаг переводятся в `STEP-006`
- [x] реализация направлена в рабочий шаг
- [x] тексты поправлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- отсутствуют

Итог:
- закрепы `Сегодня`, `Рутины`, `Цели`, `Прогресс` приведены к единому спокойному стилю старых топиков;
- видимые списки переведены на длинное тире `—`;
- `Цели` описаны как долгосрочные цели на сезон, а prompt объясняет поля формата вместо частного примера;
- недельный обзор целей и итог периода переписаны на более конкретные вопросы;
- парсер рутины теперь принимает длинное тире в пользовательском списке;
- проверки: `go test ./internal/ui ./internal/domain ./internal/bot ./internal/app/goals ./internal/app/routine`, `make test`, `make lint`.

### Review Delta: External Copy Review

ID источника: `S006`

Исходный ввод:

```text
Приложен файл `/Users/igor/.codex/attachments/94d461f5-f57c-4b41-89ca-515bccdee362/pasted-text.txt` с ревью текстов Trackmate.
```

Нормализация:
- [x] требование: убрать конструкцию `цель-задача дня` из видимых текстов и закрепить термин `задача дня` для ежедневного сценария.
- [x] требование: уплотнить карточки задачи и прогресса, убрав лишние пустые строки перед `<blockquote>` после заголовков `План` и `Итог`.
- [x] требование: смягчить редкие вставки про сезонные цели, убрать назидательные слова вроде `провал` и формулировки вроде `двигает тебя`.
- [x] требование: заменить видимый заголовок `Рутины: таблица` на более естественный `Таблица рутин`.
- [x] требование: структурировать шаблон целей через маркированные поля `Результат`, `Метрика`, `Шаг недели`, `Зачем`.
- [x] конфликт: ревью предложило пример `оффер Go/backend`; не принимаем, потому что это нарушает запрет на англицизмы из S005.

Маршрутизация:
- [x] карта источников обновляется в `S006`
- [x] чеклист обновляется в `REQ-020`
- [x] план и текущий шаг переводятся в `STEP-005`
- [x] реализация направлена в рабочий шаг
- [x] тексты поправлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- S006 уточняет S001/S005: для дневного сценария используем `задача дня`, а не `цель-задача дня`.
- S006 предлагает англицизм в примере цели; отклонено в пользу русского примера.

Итог:
- `Сегодня` теперь говорит про главную задачу дня, без `цель-задача`;
- карточки задач и прогресса получили `План:` без лишней пустой строки перед цитатой;
- вставки про цели стали нейтральнее: `Связана ли эта задача...`, `Как это событие влияет...`;
- таблица рутины называется `Таблица рутин`;
- шаблон целей структурирован маркерами, пример оставлен без `оффер Go/backend`;
- проверки: `go test ./internal/ui ./internal/bot ./internal/app/goals ./internal/app/routine ./internal/storage/postgres`, `make test`, `make lint`.

### Product Copy Delta: Message Catalog And Routine Input

ID источника: `S014`

Исходный ввод:

```text
сделай пример с дефисами, проверь что формтаиорвание и стиль сообщений такой же как в топике сегодня сделай сообщения проще и понятнее, также нужно сдлеать чтобы все сообщения в боте были в одном месте в одном документе откуда импортировались чисто, а не в коде - чтобы их можно было в одном месте менять править и оттуда брать на редактуру и потом просто импортить

рутина она одна ежедневная в ней могут быть пункты задачи что угодно и пример сделай с дефисами и пусть поддерживает только дефисы ну или тире как правильно там про максимум не пиши пока не будет проблемы

проблемы все тоже в топиках отмечай как в сегодня - чтобы была кнопка действия или понял которая удаляет сообщение и чтобы псоде исправления или действия чтобы все пропадало чтобы не было
```

Нормализация:
- [x] требование: вынести пользовательские тексты и кнопки в один редактируемый документ и импортировать их из кода.
- [x] требование: упростить тексты рутин и привести стиль к `Сегодня`.
- [x] требование: пример рутины показывать с дефисами; routine input принимает только строки с `-` или `—`.
- [x] требование: не писать про максимум в routine prompt; лимит остается защитой, но показывается только если пользователь реально уперся в проблему.
- [x] требование: служебные problem/notice-сообщения получают кнопку `Понял` или действие и удаляются после действия/закрытия.

Маршрутизация:
- [x] карта источников обновляется в `S014`
- [x] чеклист обновляется в `REQ-032..REQ-034`
- [x] план и текущий шаг переводятся в `STEP-015`

Конфликты:
- S001 просил принимать свободный список, маркеры и номера; S014 заменяет это для рутины: поддерживаем только дефисы/длинное тире.

### Routine Topic Cleanup Delta

ID источника: `S015`

Исходный ввод:

```text
надо исправить сообщение с сохранением рутин мне стиль не нравится плохой

удалять сообщение пользователя также надо после забива рутин, но надо чтобы его рутины присылались и пусть короче тогда ваще топик пустой будет кроме лидерборда то есть токо он в закрепе остальное висит токо

ладно нумерацию пусть тоже принимает трекмейт типо 1. 2. 3. но без разницы че там за номера по порядку они идут или нет трекмейт просто как начало пункта берет

в итоге в топике у нас висит таблица и трекмейт присылает рутины и просит на них ответ после ответа все удаляется и ждется новая рутина сохранение сообщений не надо
```

Нормализация:
- [x] требование: после успешной настройки рутины не оставлять сообщение `Рутина сохранена` или другое confirmation в topic.
- [x] требование: сообщение пользователя со списком рутины удаляется после сохранения.
- [x] требование: daily routine card остается временной: после полного ответа удаляется, а постоянной остается только таблица рутин.
- [x] требование: автозакрытие рутины чистит карточку/напоминание/pending prompt без отдельного notice.
- [x] требование: parser рутины снова принимает нумерацию `1.`/`2)` как префикс пункта, порядок номеров не валидируется.

Маршрутизация:
- [x] карта источников обновляется в `S015`
- [x] чеклист обновляется в `REQ-033`, `REQ-035`
- [x] план и текущий шаг переводятся в `STEP-016`

Конфликты:
- S015 заменяет часть S014: нумерация снова принимается, но пример в prompt остается с дефисами.
- S015 заменяет часть S014/REQ-034: вместо dismissable routine save confirmation теперь confirmation не оставляется вообще.

### Production Bug Delta: Legacy Pending Constraint

ID источника: `S013`

Исходный ввод:

```text
вот это прям надо поправить на проде что егор отправил сообщ но оно не прикрепилось потому что у него был открыт итог дня в другом топике

надо сначала прикрепить его сообщ к задаче и почистить мусор который там в сообщах остался, писать не надо ниче просто прикрепить чтобы в бд трекалось

второе - отследить баг и понять почему такое произошло когда вроде бы топики у нас а) изолированы друг длруга и ввод не должен мешать друг-другу вообще и б) если в одном и том же топике начинается повторный ввод одного и того же, то незаконченный ввод должен сбрасываться и все сообщения удаляться
```

Нормализация:
- [x] требование: точечно исправить production запись Егора без новых сообщений в чат.
- [x] требование: понять, почему topic-scoped pending не сработал.
- [x] требование: сохранить schema-fix в миграциях, чтобы старый `workspace/user` constraint не блокировал pending в разных топиках.
- [x] ограничение: не деплоить локальную пачку routine fixes вместе с этим production cleanup.

Маршрутизация:
- [x] карта источников обновляется в `S013`
- [x] чеклист обновляется в `REQ-031`
- [x] план и текущий шаг переводятся в `STEP-014`
- [x] production данные исправлены точечно
- [x] локальная миграция добавлена

Конфликты:
- Удалить старые Telegram-сообщения `3385` и `3404` не удалось: Bot API вернул `Bad Request: message can't be deleted`, сообщения старше окна удаления.

Итог:
- `daily_tasks.id=160` на production исправлен на `done`, итог привязан к `report_message_id=3386`;
- неверный `progress_events.id=183` удален, новая публикация в `Прогресс` не создавалась;
- старая карточка задачи `3361` отредактирована в состояние `выполнена`;
- с production снят legacy constraint `uq_pending_inputs_workspace_group_id`;
- добавлена локальная миграция `202606290001_drop_legacy_pending_input_unique.sql`.

### Review Delta: Russian Copy And Visual Formatting

ID источника: `S005`

Исходный ввод:

```text
из правок надо исправить форматирования и формулировки поправить всех сообщения чтобы они были приятнее глазу примерно как сообщения в том что было до этого то есть все сообщения сделать с приятным глазу форматирование лаконичным в стиле трекмейта, добавить отступы форматирования правильные, буквы большие где надо, где надо убрать точки и тп, короче сделать нормально

дальше надо убрать английские слова и англицизмы из терминов и прочего, от трекмейта они вообще не нужны нигде практически
```

Нормализация:
- [x] требование: переписать пользовательские сообщения бота в лаконичном стиле Trackmate.
- [x] требование: привести визуальное форматирование карточек к более аккуратным отступам, заголовкам и спискам.
- [x] требование: убрать англицизмы из пользовательских терминов: `check-in`, `leaderboard`, `review`, `daily`, `weekly`, `outcome`, `стрик` и похожие.
- [x] граница: внутренние callback/data/API/enum имена не переименовывать ради текста, чтобы не ломать рабочие контракты.

Маршрутизация:
- [x] карта источников обновляется в `S005`
- [x] чеклист обновляется в `REQ-019`
- [x] план и текущий шаг переводятся в `STEP-004`
- [x] реализация направлена в рабочий шаг
- [x] тексты поправлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- отсутствуют

Итог:
- пользовательские сообщения Today/Routine/Goals/Progress/alerts приведены к более короткому русскому стилю;
- видимые термины заменены: `check-in` -> `проверка`, `leaderboard` -> `таблица`, `review` -> `обзор`/`итог периода`, `стрик` -> `серия`, `status` -> `состояние`, `report` -> `итог`;
- кнопка ежедневной задачи стала `🏁 Подвести итог`;
- E2E-шаблоны обновлены под новые видимые тексты;
- проверки: `go test ./internal/ui ./internal/bot ./internal/app/goals ./internal/app/routine`, `make test`, `make lint`.

### Production Progress Correction Delta

ID источника: `S017`

Исходный ввод:

```text
вот это надо проверить сделали мы или нет и исправить и поправить чтобы в бд правильное отображение было
```

Скриншот:
`/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-ae9fb7f7-38dc-49e9-9f94-2e7767bd7cb8.png`

Нормализация:
- [x] требование: проверить production-БД по кейсу Егора с голосовым итогом `3386`.
- [x] требование: если отображение в БД неполное, исправить production-данные точечно.
- [x] ограничение: не выкатывать локальную пачку routine fixes вместе с этим production data-fix.

Итог:
- [x] `daily_tasks.id=160` уже был в состоянии `done`, `report_status=done`, `report_message_id=3386`, `failed_at=NULL`.
- [x] связанного `progress_events.daily_task.closed` для `daily_task_id=160` не было.
- [x] перед правкой сделан backup: `/opt/trackmate/backups/trackmate_manual_progress_fix_20260629T111509Z.dump`.
- [x] попытка переиспользовать старое message `3404` через Bot API не прошла: `message to edit not found`.
- [x] создан штатный `progress_events.id=192` с `event_type=daily_task.closed`, `status=done`, `report_html=Голосовое сообщение`.
- [x] worker опубликовал event в `Прогресс`: Telegram message `3649`.

Маршрутизация:
- [x] карта источников обновляется в `S017`
- [x] чеклист обновляется в `REQ-037`
- [x] план и текущий шаг переводятся в `STEP-018`
- [x] production данные исправлены
- [x] handoff обновлен

Конфликты:
- S017 дополняет S013/STEP-014: предыдущая правка восстановила саму задачу, но не создала закрывающий progress event.

### Routine Reminder Alert Delta

ID источника: `S016`

Исходный ввод:

```text
исправь стиль сообщения про напоминание про рутины стиль тоже плохой мне не нравится не как в сегодня и написано непонятным языком это надо проверять вообще во всех рутинах что все лаконично просто и понятно

также тут кнопка должна быть типо понял или отчиттаься или что-то такое короче в общем как в сегодня - этот алерт не должен висеть вечно и после закрытия рутины он тоже должен пропадать условно сутки у него срок, а алерт который показывает что рутина не выполнена и автоматом закрыта тоже пусть сутки висит
```

Нормализация:
- [x] требование: routine reminder должен быть коротким, понятным и ближе по стилю к сообщениям `Сегодня`.
- [x] требование: routine reminder должен иметь dismiss/action кнопку, чтобы не висеть как постоянный мусор.
- [x] требование: reminder удаляется после закрытия рутины и автоматически чистится примерно через сутки.
- [x] требование: при автозакрытии рутины появляется короткий notice о том, что неотмеченные пункты стали невыполненными.
- [x] требование: auto-close notice тоже временный и чистится примерно через сутки.
- [x] ограничение: не деплоить эту пачку на production без отдельной команды.

Маршрутизация:
- [x] карта источников обновляется в `S016`
- [x] чеклист обновляется в `REQ-034..REQ-036`
- [x] план и текущий шаг переводятся в `STEP-017`
- [x] реализация направлена в рабочий шаг
- [x] focused tests выполнены
- [x] handoff обновлен

Конфликты:
- S016 заменяет часть S015: auto-close снова отправляет notice, но он временный, dismissable и удаляется cleanup через сутки.

### Live E2E Delta: Test Bot Review Examples

ID источника: `S004`

Исходный ввод:

```text
теперь сделай полное е2е тестирование на тестовом боте нового функционала будут ошибки чини, после е2е оставь пример каждого воркфлоу для топика как это будет выглядеть чтобы я посмотрел и оценил текст и вид

можешь делать скрины или скринкаст работы или как-то показать просто в телеграме не удалять сообщ
```

Нормализация:
- [x] требование: выполнить live E2E на тестовом Telegram-боте по новому функционалу.
- [x] требование: найденные ошибки чинить и повторять failing сценарии.
- [x] требование: после E2E оставить видимые примеры workflow в Telegram по каждому топику.
- [x] ограничение: не запускать cleanup/delete для сообщений, чтобы Игорь мог посмотреть тексты и внешний вид.

Маршрутизация:
- [x] карта источников обновляется в `S004`
- [x] чеклист обновляется в `VAL-005`, `REQ-018`
- [x] план и текущий шаг переводятся в `STEP-003`
- [x] реализация направлена в рабочий шаг
- [x] E2E выполнен
- [x] handoff обновлен

Конфликты:
- `99-cleanup-visible-messages` существует в suite, но текущий S004 запрещает удалять сообщения; cleanup не запускаем.

Итог live E2E:
- тестовая группа: `тестирование trackmate v2`, bot api chat id `-1003871708263`, MTProto target `3871708263`;
- темы: `Сегодня` topic `10`, `Прогресс` topic `11`, `Рутины` topic `339`, `Цели` topic `340`;
- пройдены setup, Today create/report/dedup/edit/progress/alert, Routine configure/check-in/leaderboard, Goals configure/weekly/final, deterministic goal nudge;
- найдено и исправлено: brittle wait в routine/goals E2E заменен на visible-text assert; final review goals теперь сравнивает дату окончания периода как локальную дату workspace;
- оставлены видимые Telegram-примеры во всех темах, cleanup/delete не запускался после финального review-state dump.

### Review Delta: Fairness, Cooldown, Architecture, Full Testing

ID источника: `S003`

Исходный ввод:

```text
Пятое: leaderboard по streak сам по себе нечестный, если у одного человека 1 пункт, а у другого 8. Для вашего маленького круга это терпимо, но в MVP я бы показывал сразу две метрики: текущий streak и completion rate за 7 дней. Тогда человек с одной лёгкой привычкой не выглядит абсолютным победителем только за счёт простоты.

Шестое: random triggers по целям лучше делать не совсем случайными 10%. Случайность в продукте часто ощущается как хаос. Лучше сделать “редкие мягкие вставки” с deterministic throttle: не чаще 1 раза в 3 дня на пользователя и только если у него есть активные цели. Внутри можно оставить pseudo-random, но с cooldown.

Главный риск не в продукте, а в реализации: текущий бот маленький и явный, но уже имеет callbacks, pending inputs, worker transitions, topic bindings, progress outbox и Telegram message editing. Если просто скопировать today-flow два раза, код начнёт быстро гнить. Это прямо видно по текущей архитектуре: internal/bot сейчас маршрутизирует setup, Today, reports и alert acknowledgements; internal/app/today отвечает за daily task rules; internal/app/progress — за progress outbox. Новые домены лучше делать отдельными пакетами, а не раздувать internal/bot/service.go.

есть еще несколько замечаний их учти тоже

докер запустил - запиши что если надо можешь запускать докер сам

сделай полное тестирование проверь что код покрыт адекватными тестами
```

Нормализация:
- [x] требование: leaderboard явно показывает current streak и 7-day completion rate; ranking не выглядит чистым streak-only.
- [x] требование: вставки про цели имеют детерминированное ограничение не чаще 1 раза в 3 дня на пользователя и работают только при активных целях.
- [x] требование: новые домены отделяются от раздутого `internal/bot/service.go`; worker делегирует routine/goals app packages.
- [x] решение: Docker локально доступен, можно запускать Docker-команды для тестирования.
- [x] валидация: выполнить PostgreSQL-backed `go test ./...`, `make lint`, `make test`, локальную миграцию.

Маршрутизация:
- [x] исходный ввод сохранен или записан здесь
- [x] карта источников обновляется в `S003`
- [x] чеклист обновляется в `REQ-015..REQ-017`, `VAL-004`
- [x] план и текущий шаг переводятся в `STEP-002`
- [x] реализация направлена в рабочий шаг
- [x] готово к пользовательскому ревью текущего diff
- [x] handoff обновлен

Конфликты:
- отсутствуют
