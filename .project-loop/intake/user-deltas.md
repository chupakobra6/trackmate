# Пользовательские Дельты

Проект: trackmate
Обновлено: 2026-06-29

Этот файл хранит существенные свежие корректировки, решения и изменения области от пользователя, которые приходят во время шага. Записи компактны и пополняются до отражения в источнике правды и handoff.

## Записи

### Review Delta: Routine Reason Prompt Visual Consistency

ID источника: `S011`

Приложение: `/var/folders/70/xq5yx2813j1c27f2xf1mjkxw0000gn/T/codex-clipboard-c88a6f65-65d8-4919-851d-90137a4b96e9.png`.

Исходный ввод:

```text
смотри щас буду присылать скриншоты там где трекмейт повел себя не так как я хотел и это надо будет проверять и править

проверять по логам надо прям на продакшене и смотреть сообщения + править эти правки и баги но только детермининрованно и только то что я сказал все сразу править и чистить не надо - будем по порядку все делать

сейчас надо проверить по коду на продакшене и исправить на локалке - пока на прод не грузим исправления всей пачкой загрузим - вот этот момент на скриншоте тут почему-то рутина по разному отображается

2 - надо изменить эмодзи который идет первым в рутине на что-то другое более приятнео глазу
```

Нормализация:
- [x] требование: проверить production logs/DB для конкретного поведения на скриншоте.
- [x] требование: не делать production deploy сейчас; исправления остаются локально до пачки.
- [x] требование: исправить только показанную проблему, не чистить и не править остальные найденные данные.
- [x] требование: prompt причины рутины должен выглядеть как обычная карточка той же рутины, включая автора и формат вопроса.
- [x] требование: заменить первый emoji в routine card/control header на более приятный визуально.

Маршрутизация:
- [x] карта источников обновляется в `S011`
- [x] чеклист обновляется в `REQ-029`
- [x] план и текущий шаг переводятся в `STEP-012`
- [x] production logs/DB проверены read-only
- [x] локальная реализация направлена в `internal/ui/formatters.go`, `internal/bot/routines.go`
- [x] тесты обновлены
- [x] проверка выполнена
- [x] handoff обновлен

Продовые факты:
- production host `/opt/trackmate` работает на `9a58215`.
- `2026-06-24 17:00:04 UTC` worker отправил routine cards `3373` и `3374` в topic `3283`.
- callback `routine:item:287:0:failed` пришел по message `3373`, затем user message `3375`; callback `routine:item:287:1:failed` пришел по тому же message `3373`.
- Разница на скриншоте вызвана `FormatRoutineReasonPrompt`: он редактировал ту же карточку, но строил текст отдельным formatter'ом без `personLabel` и без обычного формата `N/M: пункт?`.

Итог:
- Reason prompt рутины теперь переиспользует обычную карточку рутины, поэтому сохраняет автора и формат вопроса `N/M: пункт?`.
- Короткая строка `Что помешало?` добавляется к той же карточке без отдельного визуального шаблона.
- Первый emoji routine header заменен с `🔁` на `🌿`.
- Production deploy не выполнялся.
- Проверки: `go test ./internal/ui ./internal/bot`, `go test ./... -count=1`, `make lint`, `loopctl.py validate`.

### Review Delta: Routine Card Date Clarity

ID источника: `S010`

Исходный ввод:

```text
Смотри, сейчас пришли две рутины, получается, на продакшене. Вот, и такой прикол, что там у них, типа, во-первых, нет описания никакого текста или типа того, во-вторых, они просто написаны 24.06, и кажется такое впечатление, что эти рутины, они пришли за 24 число, которое только началось, а не за 23, которое уже прошло. Вот, и я сейчас даже сам уже сам не понимаю, это рутины я сейчас должен отметить за прошлый день, или это на сегодня рутина. Вот, объясни мне этот момент, вот, и добавь какое-то или объяснение, или что вот в саму карточку рутины, которая прилетает, такое лаконичное, краткое, что, типа, это там за сегодня или за какое число. Ну, то есть, можно не точку, а можно просто объяснить, что типа там в фейк 13, типа этот там отметь там свою, как прошла там твоя рутина вчера, сегодня там и так далее.
```

Нормализация:
- [x] требование: объяснить, что дата в карточке рутины означает дату самой проверки рутины.
- [x] требование: сделать карточку рутины самодостаточной и явно писать, за какую дату нужно отмечать пункты.
- [x] требование: текст должен быть лаконичным и в стиле Trackmate.

Маршрутизация:
- [x] карта источников обновляется в `S010`
- [x] чеклист обновляется в `REQ-028`
- [x] план и текущий шаг переводятся в `STEP-011`
- [x] реализация направлена в `internal/ui/formatters.go`
- [x] тесты обновлены
- [x] проверка выполнена
- [x] handoff обновлен

Итог:
- На текущем production дата `24.06` в карточке означает routine check-in за `24.06`, а не за `23.06`.
- В локальной версии карточка теперь начинается с `Рутина за DD.MM` и содержит короткую строку `Отметь, как прошел этот день`.
- Такой же смысл добавлен в prompt причины, чтобы при `Нет`/`Частично` дата не терялась.
- Проверка: `go test ./internal/ui ./internal/bot ./internal/app/routine`.

### Review Delta: Topic-Scoped Pending Inputs And Evening Routine Flow

ID источника: `S009`

Исходный ввод:

```text
Давай для всех топиков сделаем поведение, как для топика сегодня. То есть, если бот ждет там цель или рутину, то пусть он, короче, ждет, а топик в другой ввод, он игнорируется. Вот. При этом давай добавим fallback, ну, короче, проверку, что если там сутки ввода никакого нет, то бот отменяет input pending и как бы удаляет все сообщения, которые были вот как бы в процессе, да, то есть, типа, введи там или что-то там от пользователя что-то. Вот. То есть сделаем каждый топик таким изолированным, просто чтобы каждый топик вел себя одинаково. Вот. То есть мы в каждом топике пишем, вот, и то, что мы пишем в каждом топике, оно не влияет на другие топики. И если мы, допустим, в одном топике начали писать, а продолжили в другом, то у нас всё в другом так же работает и не мешает, и первый топик не мешает второму, и наоборот, ввод во второй топик не сломает там первый топик.

Не очень понимаю историю про то, что у пользователя только один незавершенный ввод на группу. Не очень понятно, зачем это. Мне кажется, такого не должно быть. Мы просто по топикам думаем, то есть внутри топика, в рамках топика. То есть, если pending input, то окей, он есть. Вот типа, допустим, с fallback там в сутки, да. Вот. Если pending input нет, то как бы другие... Вообще они как бы не должны друг на друга влиять, разные топики, они не должны игнорировать события с других топиков. Топик должен смотреть на события только со своего топика. По умолчанию. То есть, не должно быть ни ограничений, он по умолчанию должен смотреть только на события со своего топика. Топики в целом, они похожи все. То есть там код можно переиспользовать, обобщать и таким образом поддерживать качество кода. Если что-то отменяется, если trackmate удаляет там pending input, удаляет там свои сообщения, то он это должен делать абсолютно молча, без каких-либо там сообщений в ответ и так далее. То есть мы унифицируем flow, как для daily. Да, дополнительная публикация карточки с полным текстом цели не нужна. Давай потом поменяем, чтобы карточка рутины приходила... Например, давай пусть карточка, во-первых, если по времени просто проходит, то она приходит в тот же день, а не обязательно с следующего дня. Ну как бы если по времени позволяет, да, если она там раньше времени. А во-вторых, давай, наверное, сделаем, чтобы она вечером в день приходила, то есть в день, ну как бы вечером. И плюс она также её нужно было бы закрыть, допустим, до 12 часов следующего дня. То есть там в 12.00 по Москве там придёт сообщение, типа, рутина не закрыта. Ну вот как в сегодня, точно такой же flow, то есть она придёт просто вечером, вот. Потом в 12 придёт сообщение, что типа закрою рутину, а потом в 12 часов следующего дня он тебе типа сам закроет, что она не выполнена, грубо говоря.
```

Нормализация:
- [x] требование: `pending_inputs` должны быть изолированы по теме: один пользователь может иметь независимый незавершенный ввод в разных топиках.
- [x] требование: сообщения из другой темы не отменяют и не ломают pending текущей темы; обработчик смотрит только pending той темы, где пришло сообщение/callback.
- [x] требование: убрать глобальный запрет "один pending input на workspace/user".
- [x] требование: pending input старше 24 часов тихо отменяется worker'ом; Trackmate удаляет свой prompt и известные сообщения пользователя из процесса без callback/message ответа.
- [x] требование: настройка целей остается без большой карточки с полным текстом целей.
- [x] требование: routine check-in приходит вечером, в день настройки тоже, если рутина настроена до вечернего времени.
- [x] требование: незакрытая рутина получает напоминание после конца дня и автоматически закрывается в 12:00 следующего дня как невыполненная.

Маршрутизация:
- [x] карта источников обновляется в `S009`
- [x] чеклист обновляется в `REQ-025..REQ-027`
- [x] план и текущий шаг переводятся в `STEP-010`
- [x] реализация направлена в рабочий шаг
- [x] тесты добавлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- `S008` требовал сброса setup-черновиков при переходе между `Рутины` и `Цели`; новая дельта `S009` выше по приоритету и заменяет это поведение изоляцией по теме без сброса чужого топика.

Итог:
- `pending_inputs` теперь имеют явный `message_thread_id` и уникальность `workspace/user/thread`;
- сообщения и callbacks в одном топике не блокируются pending из другого топика;
- wrong-topic ввод больше ничего не удаляет и не сбрасывает;
- worker тихо удаляет pending старше 24 часов и удаляет сохраненные prompt/user message IDs;
- рутина приходит после 20:00; если список сохранен до 20:00, первая карточка может прийти в тот же день;
- незакрытая рутина получает напоминание после конца дня и автозакрывается в 12:00 следующего дня;
- проверки без доступной PostgreSQL: `go test ./internal/domain ./internal/storage/postgres ./internal/app/pending ./internal/app/routine ./internal/app/goals ./internal/worker ./internal/bot ./internal/ui`, `go test ./... -count=1`, `make test`, `make lint`;
- DB-backed integration tests и `TRACKMATE__DATABASE_URL=... make migrate` не выполнены полноценно: локальный PostgreSQL недоступен, `make docker-up` не смог подключиться к Docker daemon.

### Review Delta: Pending Setup Cleanup And Goals Echo

ID источника: `S008`

Исходный ввод:

```text
трекмейт нужно чтобы прошлый ввод сбрасывался рутины или цели и удалялись сообщения от трекмейта и юзера если ввод не закончен и если юзер начал делать ввод в другом топике

почему на цели трекмейт присылает большое полотно то есть переписывает твои цели в сообщении. только первый забив потом не присылает уже твои цели и просто пишет что цели записаны и по другому отвечает - надо одним способом как-то

во сколько трекмейт присылает отчет по рутинам?

исправь эти вещи
```

Нормализация:
- [x] требование: незаконченные setup-черновики `routine_plan` и `seasonal_goals` можно перебивать новым setup-вводом в `Рутины`/`Цели`.
- [x] требование: при переходе в другой топик старый prompt Trackmate удаляется, wrong-topic сообщение пользователя удаляется, pending input очищается.
- [x] требование: сохранение целей не должно публиковать большую карточку с полным текстом целей; первый и повторный ввод отвечают одинаково коротко.
- [x] требование: объяснить и явно показать время routine check-in: после 09:00 по локальному времени workspace.

Маршрутизация:
- [x] карта источников обновляется в `S008`
- [x] чеклист обновляется в `REQ-022..REQ-024`
- [x] план и текущий шаг переводятся в `STEP-009`
- [x] реализация направлена в рабочий шаг
- [x] тесты добавлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- wrong-topic behavior для daily input остается прежним: daily pending не закрывается сообщением из другой темы. S008 применяется только к setup-черновикам `Рутины`/`Цели`.

Итог:
- `routine_plan` и `seasonal_goals` теперь можно перебить новым setup-вводом в `Рутины`/`Цели`;
- при wrong-topic сообщении для такого черновика удаляются старый prompt Trackmate и сообщение пользователя, pending input очищается;
- сохранение целей больше не создает отдельную большую карточку с полным текстом целей, confirmation единый и короткий;
- старые goal card messages удаляются при следующем сохранении целей;
- routine check-in явно описан как ежедневный после 09:00 по локальному времени workspace;
- проверки: `go test ./internal/bot ./internal/ui ./internal/storage/postgres ./internal/domain ./internal/app/routine ./internal/app/goals`, `make test`, `make lint`, `loopctl.py validate`.

### Review Delta: Topic Copy Tone And Goal Questions

ID источника: `S007`

Исходный ввод:

```text
Сезонные цели на общий период - тут исправь на чтониьудь понятнее описание как в других топиках что то вроде долгосрочные цели которых мы хотим добитьсся за лето напримери

стиль предложений разный у новых и старых топиков описаний надо привести под стиль старых

маркеры - тире а не маркеры

пример формата в сезонные цели чуть лучше сделай. и лучше не пример а описание полей типо смарт что там в полях зачем

в недельном обзоре целей надо переформулировать вопросы оч старнные

🏁 <b>Итог периода: Лето 2026</b>

<b>Оценка:</b> частично

Напиши короткий вывод:
• что получилось
• что не получилось
• что берешь в следующий сезон - в итогах периода не короткий вывод а прям конкретный

вот исправление примени
```

Приложение: `/Users/igor/.codex/attachments/55bd8dc4-45f5-4892-8186-d844e12eeb30/pasted-text.txt`.

Нормализация:
- [x] требование: привести закрепы новых топиков к стилю старых: `Здесь у каждого...`, `Здесь живут...`, `Здесь собирается...`.
- [x] требование: заменить видимые маркеры списков с `•` на длинное тире `—`.
- [x] требование: описание `Цели` сделать понятнее: долгосрочные цели/ориентиры на сезон, например на лето.
- [x] требование: вместо частного примера целей дать описание полей формата: направление, результат, метрика, шаг недели, зачем.
- [x] требование: переписать вопросы недельного обзора целей на естественные и конкретные.
- [x] требование: итог периода просит конкретные результаты по целям, а не короткий вывод.
- [x] требование: парсер рутины должен принимать длинное тире, раз интерфейс предлагает такой стиль списков.

Маршрутизация:
- [x] карта источников обновляется в `S007`
- [x] чеклист обновляется в `REQ-021`
- [x] план и текущий шаг переводятся в `STEP-006`
- [x] реализация направлена в рабочий шаг
- [x] тексты поправлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- отсутствуют

Итог:
- закрепы `Сегодня`, `Рутины`, `Цели`, `Прогресс` приведены к единому спокойному стилю старых топиков;
- видимые списки переведены на длинное тире `—`;
- `Цели` описаны как долгосрочные цели на сезон, а prompt объясняет поля формата вместо частного примера;
- недельный обзор целей и итог периода переписаны на более конкретные вопросы;
- парсер рутины теперь принимает длинное тире в пользовательском списке;
- проверки: `go test ./internal/ui ./internal/domain ./internal/bot ./internal/app/goals ./internal/app/routine`, `make test`, `make lint`.

### Review Delta: External Copy Review

ID источника: `S006`

Исходный ввод:

```text
Приложен файл `/Users/igor/.codex/attachments/94d461f5-f57c-4b41-89ca-515bccdee362/pasted-text.txt` с ревью текстов Trackmate.
```

Нормализация:
- [x] требование: убрать конструкцию `цель-задача дня` из видимых текстов и закрепить термин `задача дня` для ежедневного сценария.
- [x] требование: уплотнить карточки задачи и прогресса, убрав лишние пустые строки перед `<blockquote>` после заголовков `План` и `Итог`.
- [x] требование: смягчить редкие вставки про сезонные цели, убрать назидательные слова вроде `провал` и формулировки вроде `двигает тебя`.
- [x] требование: заменить видимый заголовок `Рутины: таблица` на более естественный `Таблица рутин`.
- [x] требование: структурировать шаблон целей через маркированные поля `Результат`, `Метрика`, `Шаг недели`, `Зачем`.
- [x] конфликт: ревью предложило пример `оффер Go/backend`; не принимаем, потому что это нарушает запрет на англицизмы из S005.

Маршрутизация:
- [x] карта источников обновляется в `S006`
- [x] чеклист обновляется в `REQ-020`
- [x] план и текущий шаг переводятся в `STEP-005`
- [x] реализация направлена в рабочий шаг
- [x] тексты поправлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- S006 уточняет S001/S005: для дневного сценария используем `задача дня`, а не `цель-задача дня`.
- S006 предлагает англицизм в примере цели; отклонено в пользу русского примера.

Итог:
- `Сегодня` теперь говорит про главную задачу дня, без `цель-задача`;
- карточки задач и прогресса получили `План:` без лишней пустой строки перед цитатой;
- вставки про цели стали нейтральнее: `Связана ли эта задача...`, `Как это событие влияет...`;
- таблица рутины называется `Таблица рутин`;
- шаблон целей структурирован маркерами, пример оставлен без `оффер Go/backend`;
- проверки: `go test ./internal/ui ./internal/bot ./internal/app/goals ./internal/app/routine ./internal/storage/postgres`, `make test`, `make lint`.

### Review Delta: Russian Copy And Visual Formatting

ID источника: `S005`

Исходный ввод:

```text
из правок надо исправить форматирования и формулировки поправить всех сообщения чтобы они были приятнее глазу примерно как сообщения в том что было до этого то есть все сообщения сделать с приятным глазу форматирование лаконичным в стиле трекмейта, добавить отступы форматирования правильные, буквы большие где надо, где надо убрать точки и тп, короче сделать нормально

дальше надо убрать английские слова и англицизмы из терминов и прочего, от трекмейта они вообще не нужны нигде практически
```

Нормализация:
- [x] требование: переписать пользовательские сообщения бота в лаконичном стиле Trackmate.
- [x] требование: привести визуальное форматирование карточек к более аккуратным отступам, заголовкам и спискам.
- [x] требование: убрать англицизмы из пользовательских терминов: `check-in`, `leaderboard`, `review`, `daily`, `weekly`, `outcome`, `стрик` и похожие.
- [x] граница: внутренние callback/data/API/enum имена не переименовывать ради текста, чтобы не ломать рабочие контракты.

Маршрутизация:
- [x] карта источников обновляется в `S005`
- [x] чеклист обновляется в `REQ-019`
- [x] план и текущий шаг переводятся в `STEP-004`
- [x] реализация направлена в рабочий шаг
- [x] тексты поправлены
- [x] проверка выполнена
- [x] handoff обновлен

Конфликты:
- отсутствуют

Итог:
- пользовательские сообщения Today/Routine/Goals/Progress/alerts приведены к более короткому русскому стилю;
- видимые термины заменены: `check-in` -> `проверка`, `leaderboard` -> `таблица`, `review` -> `обзор`/`итог периода`, `стрик` -> `серия`, `status` -> `состояние`, `report` -> `итог`;
- кнопка ежедневной задачи стала `🏁 Подвести итог`;
- E2E-шаблоны обновлены под новые видимые тексты;
- проверки: `go test ./internal/ui ./internal/bot ./internal/app/goals ./internal/app/routine`, `make test`, `make lint`.

### Live E2E Delta: Test Bot Review Examples

ID источника: `S004`

Исходный ввод:

```text
теперь сделай полное е2е тестирование на тестовом боте нового функционала будут ошибки чини, после е2е оставь пример каждого воркфлоу для топика как это будет выглядеть чтобы я посмотрел и оценил текст и вид

можешь делать скрины или скринкаст работы или как-то показать просто в телеграме не удалять сообщ
```

Нормализация:
- [x] требование: выполнить live E2E на тестовом Telegram-боте по новому функционалу.
- [x] требование: найденные ошибки чинить и повторять failing сценарии.
- [x] требование: после E2E оставить видимые примеры workflow в Telegram по каждому топику.
- [x] ограничение: не запускать cleanup/delete для сообщений, чтобы Игорь мог посмотреть тексты и внешний вид.

Маршрутизация:
- [x] карта источников обновляется в `S004`
- [x] чеклист обновляется в `VAL-005`, `REQ-018`
- [x] план и текущий шаг переводятся в `STEP-003`
- [x] реализация направлена в рабочий шаг
- [x] E2E выполнен
- [x] handoff обновлен

Конфликты:
- `99-cleanup-visible-messages` существует в suite, но текущий S004 запрещает удалять сообщения; cleanup не запускаем.

Итог live E2E:
- тестовая группа: `тестирование trackmate v2`, bot api chat id `-1003871708263`, MTProto target `3871708263`;
- темы: `Сегодня` topic `10`, `Прогресс` topic `11`, `Рутины` topic `339`, `Цели` topic `340`;
- пройдены setup, Today create/report/dedup/edit/progress/alert, Routine configure/check-in/leaderboard, Goals configure/weekly/final, deterministic goal nudge;
- найдено и исправлено: brittle wait в routine/goals E2E заменен на visible-text assert; final review goals теперь сравнивает дату окончания периода как локальную дату workspace;
- оставлены видимые Telegram-примеры во всех темах, cleanup/delete не запускался после финального review-state dump.

### Review Delta: Fairness, Cooldown, Architecture, Full Testing

ID источника: `S003`

Исходный ввод:

```text
Пятое: leaderboard по streak сам по себе нечестный, если у одного человека 1 пункт, а у другого 8. Для вашего маленького круга это терпимо, но в MVP я бы показывал сразу две метрики: текущий streak и completion rate за 7 дней. Тогда человек с одной лёгкой привычкой не выглядит абсолютным победителем только за счёт простоты.

Шестое: random triggers по целям лучше делать не совсем случайными 10%. Случайность в продукте часто ощущается как хаос. Лучше сделать “редкие мягкие вставки” с deterministic throttle: не чаще 1 раза в 3 дня на пользователя и только если у него есть активные цели. Внутри можно оставить pseudo-random, но с cooldown.

Главный риск не в продукте, а в реализации: текущий бот маленький и явный, но уже имеет callbacks, pending inputs, worker transitions, topic bindings, progress outbox и Telegram message editing. Если просто скопировать today-flow два раза, код начнёт быстро гнить. Это прямо видно по текущей архитектуре: internal/bot сейчас маршрутизирует setup, Today, reports и alert acknowledgements; internal/app/today отвечает за daily task rules; internal/app/progress — за progress outbox. Новые домены лучше делать отдельными пакетами, а не раздувать internal/bot/service.go.

есть еще несколько замечаний их учти тоже

докер запустил - запиши что если надо можешь запускать докер сам

сделай полное тестирование проверь что код покрыт адекватными тестами
```

Нормализация:
- [x] требование: leaderboard явно показывает current streak и 7-day completion rate; ranking не выглядит чистым streak-only.
- [x] требование: вставки про цели имеют детерминированное ограничение не чаще 1 раза в 3 дня на пользователя и работают только при активных целях.
- [x] требование: новые домены отделяются от раздутого `internal/bot/service.go`; worker делегирует routine/goals app packages.
- [x] решение: Docker локально доступен, можно запускать Docker-команды для тестирования.
- [x] валидация: выполнить PostgreSQL-backed `go test ./...`, `make lint`, `make test`, локальную миграцию.

Маршрутизация:
- [x] исходный ввод сохранен или записан здесь
- [x] карта источников обновляется в `S003`
- [x] чеклист обновляется в `REQ-015..REQ-017`, `VAL-004`
- [x] план и текущий шаг переводятся в `STEP-002`
- [x] реализация направлена в рабочий шаг
- [x] готово к пользовательскому ревью текущего diff
- [x] handoff обновлен

Конфликты:
- отсутствуют
